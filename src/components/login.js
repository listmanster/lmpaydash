import React from 'react';
import {MainBody} from '../components/layout';

export const Login = () => {
    return (
        <MainBody title="Log In">
            <h2>Login</h2>
        </MainBody>

    );
}


export const SignUp = () => {
    return (
        <MainBody title="Sign Up">
            <h2>Sign Up</h2>
        </MainBody>
    );
}